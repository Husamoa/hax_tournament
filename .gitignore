# IDE
.idea/

# Node (tylko do testów lokalnych)
node_modules/

# Lokalna konfiguracja z sekretami — NIGDY nie commitować
api/config.php

# Sekrety bota headless (token pokoju) — NIGDY nie commitować
bot/.env

# Lokalna baza SQLite (dev)
*.sqlite
*.sqlite3

# Pliki sesji PHP (własny save_path — trzymane per-instancja, nie commitować)
api/sessions/

# Paczka wdrożeniowa na OVH (zawiera config.php z hashem i bazę) — NIGDY nie commitować
deploy-ovh/

# Claude local
.claude/settings.local.json
