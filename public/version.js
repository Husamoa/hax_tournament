// Wersja aplikacji Pitole — JEDYNE miejsce do podbijania.
// Wyświetlana na ekranie logowania i w nagłówku aplikacji.
// Po zmianie: commit + push do main → auto-deploy wystawi nową wersję na pitole.pl.
// (Opcjonalnie zsynchronizuj też pole "version" w package.json.)
export const APP_VERSION = '0.4.0';
